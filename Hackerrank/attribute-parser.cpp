#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int N, Q; cin >> N >> Q;    
    vector<string> str; 
    vector<string> query; 
    for (int i = 0; i < N; i++){
        string temp;
        cin >> temp;
        str.push_back(temp); 
    }   
    for (int i = 0; i < Q; i++){
        string temp; 
        cin >> temp; 
        query.push_back(temp);
    }
    for (int i = 0; i < Q; i++){
        
    }
       
    
    
    return 0;
}